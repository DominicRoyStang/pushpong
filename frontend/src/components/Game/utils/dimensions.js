const canvasHeight = 600;
const canvasWidth = 900;
const paddleBoundSpacing = 10;
const paddleOffset = 10;
const boundWidth = 30;

export {
    canvasHeight,
    canvasWidth,
    paddleBoundSpacing,
    paddleOffset,
    boundWidth
};
